# Single-file site: no build step. Netlify serves index.html directly and
# still bundles the serverless functions in netlify/functions.

[build]
  command = "echo No build needed"
  publish = "."

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
